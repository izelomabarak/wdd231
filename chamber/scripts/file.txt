const openButtons = document.querySelectorAll('.more-link[data-modal]');
  openButtons.forEach(btn => {
    btn.addEventListener('click', () => {
      const id = btn.getAttribute('data-modal');
      const dlg = document.getElementById(id);
      if (dlg?.showModal) dlg.showModal();
    });
  });

document.querySelectorAll('.plan-modal .close-modal').forEach(b =>
    b.addEventListener('click', e => {
      const dlg = e.target.closest('dialog');
      if (dlg?.close) dlg.close();
    })
  );

  htmml

          <div>
          <!-- <h2>Membership Levels</h2> -->
          <article class="plan-card" data-delay="0">
            <h3>NP Membership</h3>
            <p>No fee for registered non-profits. Great to get involved with the community.</p>
            <button type="button" class="more-link" data-modal="modal-np" aria-haspopup="dialog">Benefits</button>
        </article>
        <article class="plan-card" data-delay="1">
            <h3>Bronze</h3>
            <p>Essential exposure for small businesses starting out.</p>
            <button type="button" class="more-link" data-modal="modal-bronze" aria-haspopup="dialog">Benefits</button>
        </article>
        <article class="plan-card" data-delay="2">
            <h3>Silver</h3>
            <p>Increased visibility, training vouchers and event discounts.</p>
            <button type="button" class="more-link" data-modal="modal-silver" aria-haspopup="dialog">Benefits</button>
        </article>
        <article class="plan-card" data-delay="3">
            <h3>Gold</h3>
            <p>Premium placement, sponsorship opportunities and spotlight features.</p>
            <button type="button" class="more-link" data-modal="modal-gold" aria-haspopup="dialog">Benefits</button>
        </article>
        </div>
        <dialog id="modal-np" class="plan-modal">
            <div>
              <h3>NP Membership — Benefits</h3>
              <button class="close-modal" value="close">Close</button>
            </div>
            <ul>
            <li>No annual fee</li>
            <li>Directory listing</li>
            <li>Access to networking events</li>
            </ul>
        </dialog>
        <dialog id="modal-bronze" class="plan-modal">
            <h3>Bronze — Benefits</h3>
            <ul>
            <li>All NP benefits</li>
            <li>Social media mentions</li>
            <li>Member newsletter inclusion</li>
            </ul>
            <button class="close-modal" value="close">Close</button>
        </dialog>
        <dialog id="modal-silver" class="plan-modal">
            <h3>Silver — Benefits</h3>
            <ul>
            <li>All Bronze benefits</li>
            <li>Training vouchers</li>
            <li>Event ticket discounts</li>
            </ul>
            <button class="close-modal" value="close">Close</button>
        </dialog>
        <dialog id="modal-gold" class="plan-modal">
            <h3>Gold — Benefits</h3>
            <ul>
            <li>All Silver benefits</li>
            <li>Homepage spotlight rotation</li>
            <li>Sponsorship opportunities</li>
            </ul>
            <button class="close-modal" value="close">Close</button>
        </dialog>
      </section>

css
@keyframes planIntro {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.plan-card {
    display: block;
    align-self: center;
    text-align: center;
    border-radius: 10px;
    background-color: var(--Accent1-Color);
    padding: 5px;
    margin: 20px;
    --i: attr(data-delay number);
    animation: planIntro .6s ease-out both;
    animation-delay: calc(var(--i, 0) * 120ms);
}